language: python
python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"
install:
    - pip install -r requirements-test.txt
    - pip install coveralls
script:
    PYTHONPATH=reusables nosetests -w test --with-coverage -vv --cover-package=reusables
after_success:
    coveralls