language: python
python:
    - "2.7"
    - "3.2"
    - "3.3"
install:
    - pip install -r requirements-test.txt
    - pip install coveralls
script:
    nosetests --with-coverage -vv --cover-package=reuse test
after_success:
    coveralls